import React from "react";
import Menu from "./Menu";
import VideoContainer from "./VideoContainer";

const Body = () => {
  return (
    <div>
      <Menu />
      <VideoContainer />
    </div>
  );
};

export default Body;
